#!/usr/bin/env python3.6


import datetime
import re

class Economy(object):
    """ The object will be filled with variables from input. """
    def __init__(self):
        pass
    '''
    def __makes_datetime(self):
        

        __date_dict = {"year": "0", "month": "0", "day": "0"}
        


        __str_regexp = '(?P<year>\d{4,4})[-| ]?(?P<month>\d{0,2})[-| ]?(?P<day>\d{0,2})'
        __re_date_pattern = re.compile(__str_regexp)


        tmp_str_date = self.date_operation.get('date_acquire', None)

        re_return = ""

        if tmp_str_date:
            re_return = __re_date_pattern.match(tmp_str_date)
            
            match_year_re = re_return.groupdict().get('year', None):


            if None in match_year_re:
                raise ValueError("Incorrect date.")

            else:

               __date_dict['year'] = match_year_re 

        self.date_operation.get('date_realize', None)
   ''' 
